# ===== 노무AI 환경변수 =====
# 이 파일을 .env 로 복사해 값을 채우세요. (배포 시에는 호스트의 환경변수 설정창에 입력)

# ── AI provider (하나 이상 설정. 여러 개면 primary 실패 시 자동 폴백) ──
# 우선순위: AI_PROVIDER 지정 시 그것 → 기본 precedence: anthropic > gemini > groq.
# 모두 비우면 AI 상담은 '데모 모드'(목업), 나머지 기능은 정상 동작.

# (선택) Anthropic Claude — https://console.anthropic.com
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-opus-4-8
#   claude-opus-4-8($5/$25) · claude-sonnet-4-6($3/$15) · claude-haiku-4-5($1/$5)

# (무료·권장) Google Gemini — https://aistudio.google.com/apikey
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
#   무료 등급 분당 한도 있음. 사업자등록 없이 개인카드로 유료 티어 전환 시 한도↑

# (무료·폴백) Groq — https://console.groq.com  (카드 불필요, 분당30·일일1000)
# Gemini 한도 초과 시 자동 폴백. 모델은 Llama/Gemma 계열.
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile

# provider 강제 지정(선택): anthropic | gemini | groq
AI_PROVIDER=

# 운영자 대시보드(/admin) 로그인 토큰. 배포 시 반드시 강력한 값으로 변경하세요.
ADMIN_TOKEN=change-this-to-a-strong-secret

# 세션 쿠키 서명 키. 비우면 ADMIN_TOKEN에서 자동 파생되지만, 별도 지정 권장.
SESSION_SECRET=

# DB 파일 경로(선택). 기본 data/app.db. 영구 디스크 마운트 시 그 경로로.
DB_PATH=

# 배포 도메인 (정적 SEO 페이지의 canonical·OG·sitemap에 사용)
# 예: https://nomu-ai.onrender.com  또는  https://내도메인.com
SITE_URL=

# 포트 (대부분의 호스트는 자동 주입하므로 비워두면 됩니다)
PORT=3000
